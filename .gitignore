# ══════════════════════════════════════════════════════════
#  这个仓库只放**代码**。业务数据、技能资产、产出物一律不进版本库。
#
#  为什么：本系统跑在甲方项目上，目录里有
#    · 工程技术报告原件与审核成果（客户资料，涉保密）
#    · 账号密码哈希与登录流水（个人信息）
#    · WorkBuddy 训练出来的技能副本（知识资产）
#  这些一旦提交，**即使后来删除，历史记录里也永久留存**。
#
#  数据备份请用 运维\backup.ps1 备到本地/移动盘，不要走 git。
# ══════════════════════════════════════════════════════════

# ── 业务数据（最重要，务必排除）──
web/data/
web/data/**
**/server.log
**/watchdog.log
**/backup-history.log

# ── 技能/知识库副本：业务资产，不进版本库 ──
skill/
skill/**

# ── 运行产物 ──
runs/
产出/
bench/结果*.jsonl

# ── 测试样本（真实报告）──
测试样本/

# ── 截图（含真实项目名与问题数）──
运维/预览/

# ── 临时/编辑器 ──
*.tmp
*.bak
*.swp
*~
Thumbs.db
desktop.ini
.vscode/
.idea/

# ── 依赖与构建（本项目零依赖，留着以防万一）──
node_modules/
dist/
__pycache__/
*.pyc

# ── 备份包（万一备到了项目内）──
*.zip
*.7z
